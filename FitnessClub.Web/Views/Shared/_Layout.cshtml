<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FitnessClub</title>
    <link rel="stylesheet" href="~/css/site.css" />
    @RenderSection("Styles", required: false)
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="container navbar-container">
                <a class="navbar-brand" href="/">FitnessClub</a>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link @(ViewContext.RouteData.Values["Controller"].ToString() == "Home" && ViewContext.RouteData.Values["Action"].ToString() == "Index" ? "active" : "")" href="/">Головна</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(ViewContext.RouteData.Values["Controller"].ToString() == "Schedule" ? "active" : "")" href="/Schedule">Розклад</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(ViewContext.RouteData.Values["Controller"].ToString() == "Membership" ? "active" : "")" href="/Membership">Абонементи</a>
                    </li>
                    <li class="nav-item">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"].ToString() == "Account" && ViewContext.RouteData.Values["Action"].ToString() == "Profile" ? "active" : "")" href="/Account/Profile">Мій профіль</a>
                        }
                        else
                        {
                            <a class="nav-link @(ViewContext.RouteData.Values["Controller"].ToString() == "Account" && ViewContext.RouteData.Values["Action"].ToString() == "Login" ? "active" : "")" href="/Account/Login">Вхід</a>
                        }
                    </li>
                </ul>
            </div>
        </nav>
    </header>

    <main role="main">
        @RenderBody()
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3 class="footer-title">FitnessClub</h3>
                    <p>Ваш шлях до здорового способу життя та гарної фізичної форми починається тут.</p>
                </div>
                <div class="footer-section">
                    <h3 class="footer-title">Навігація</h3>
                    <ul class="footer-links">
                        <li class="footer-link"><a href="/">Головна</a></li>
                        <li class="footer-link"><a href="/Schedule">Розклад</a></li>
                        <li class="footer-link"><a href="/Membership">Абонементи</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3 class="footer-title">Контакти</h3>
                    <ul class="footer-links">
                        <li class="footer-link">м. Київ, вул. Спортивна, 123</li>
                        <li class="footer-link">+380 44 123-45-67</li>
                        <li class="footer-link">info@fitnessclub.com</li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3 class="footer-title">Графік роботи</h3>
                    <ul class="footer-links">
                        <li class="footer-link">Пн-Пт: 7:00 - 22:00</li>
                        <li class="footer-link">Сб-Нд: 9:00 - 20:00</li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                &copy; @DateTime.Now.Year - FitnessClub. Всі права захищено.
            </div>
        </div>
    </footer>

    <script src="~/js/site.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>